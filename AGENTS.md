# AGENTS.md

## 프로젝트 목표
한국어 사용자가 오늘의 할 일을 직접 입력하거나 자연어·음성으로 입력하고, OpenAI API가 이를 구조화된 할 일로 바꾸는 가벼운 웹 서비스다. 같은 화면에서 최신 AI 뉴스를 확인할 수 있어야 한다.

## 기술 원칙
- 프론트엔드는 `public/`의 바닐라 HTML/CSS/JavaScript로 유지한다.
- 서버 기능은 `netlify/functions/*.mjs`로 구현한다.
- 브라우저 번들에 비밀키를 포함하지 않는다.
- OpenAI API 요청은 반드시 Netlify Function을 거친다.
- 사용자 입력 API 키는 `sessionStorage`만 사용하며 `localStorage`, 쿠키, 로그, 저장소에 남기지 않는다.
- 기본 모델은 `gpt-5.4-mini`이며 `OPENAI_MODEL`로 교체할 수 있다.
- 할 일 데이터는 현재 `localStorage`에 저장한다. 계정 동기화는 별도 단계다.

## 코드 스타일
- 외부 런타임 의존성을 최소화한다.
- 함수는 한 가지 책임을 갖고, 사용자에게 보이는 오류는 한국어로 작성한다.
- DOM 삽입 시 사용자 입력은 `textContent` 또는 escape 처리를 사용한다.
- 접근성: 버튼에는 명확한 레이블, 대화상자에는 제목, 키보드 포커스가 있어야 한다.
- 반응형 레이아웃은 320px 너비부터 동작해야 한다.

## 완료 조건
변경 후 다음을 확인한다.
1. `npm run check`
2. 직접 할 일 추가·완료·삭제·필터
3. API 키 저장·삭제 및 탭 종료 후 제거
4. AI 자연어 입력이 여러 할 일로 분리되는지
5. 음성 미지원 브라우저에서 버튼이 안전하게 비활성화되는지
6. AI 뉴스 실패 시 오류 상태가 보이는지
7. 모바일 메뉴와 모달 키보드 사용성
